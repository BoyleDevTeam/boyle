---
applyTo: "**/*"
---
# TODO format

Use:

```
// TODO(github_username): concise task [optional reference]
```

In Python, use `#` instead of `//`.

Include a real GitHub username. Add an issue or PR reference when helpful. Remove the TODO when finished.
